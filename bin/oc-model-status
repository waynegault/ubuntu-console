#!/usr/bin/env bash
# Thin wrapper — delegates to the 'ocms' function (OpenClaw registry sync) via tac-exec.
# NOTE: despite the filename it does NOT read live status; use `model status`.
exec ~/ubuntu-console/bin/tac-exec ocms "$@"
# end of file
